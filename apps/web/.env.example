# apps/web/.env.example
# This file mirrors apps/web/.env keys and comments. Copy to .env and fill real values.
# Localhost mapping required for the local domain below:
# Add to your hosts file (e.g., /etc/hosts on macOS/Linux):
# 127.0.0.1    knowyourdev.local

# full API endpoin url
VITE_KYD_API_ENDPOINT=http://knowyourdev.local:5050/api
# this is just an ID in general sense - so it'd regardless of the real url
VITE_AUTH0_API_AUDIENCE=https://knowyourdev.local:5050
# find those in Auth0 dashboard:
VITE_AUTH0_DOMAIN=your-tenant.eu.auth0.com
VITE_AUTH0_CLIENT_ID=your-auth0-mock-client-id
